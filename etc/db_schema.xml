<?xml version="1.0"?>
<schema xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:Setup/Declaration/Schema/etc/schema.xsd">
    <table name="shopwhizzy_stripehostedcheckout_session" resource="default" engine="innodb"
           comment="ShopWhizzy Stripe Hosted Checkout Session Map">
        <column xsi:type="int" name="entity_id" unsigned="true" nullable="false" identity="true"
                comment="Entity ID"/>
        <column xsi:type="varchar" name="session_id" nullable="false" length="255" comment="Stripe Checkout Session ID"/>
        <column xsi:type="int" name="quote_id" unsigned="true" nullable="false" identity="false"
                comment="Quote ID"/>
        <column xsi:type="smallint" name="store_id" unsigned="true" nullable="false" identity="false"
                comment="Store ID"/>
        <column xsi:type="varchar" name="status" nullable="false" length="32" default="pending"
                comment="Status: pending, completed, expired, cancelled"/>
        <column xsi:type="int" name="order_id" unsigned="true" nullable="true" identity="false"
                comment="Order ID"/>
        <column xsi:type="timestamp" name="created_at" on_update="false" nullable="false"
                default="CURRENT_TIMESTAMP" comment="Created At"/>
        <column xsi:type="timestamp" name="updated_at" on_update="true" nullable="false"
                default="CURRENT_TIMESTAMP" comment="Updated At"/>
        <constraint xsi:type="primary" referenceId="PRIMARY">
            <column name="entity_id"/>
        </constraint>
        <constraint xsi:type="unique" referenceId="SHOPWHIZZY_STRIPEHOSTEDCHECKOUT_SESSION_SESSION_ID">
            <column name="session_id"/>
        </constraint>
        <index referenceId="SHOPWHIZZY_STRIPEHOSTEDCHECKOUT_SESSION_QUOTE_ID" indexType="btree">
            <column name="quote_id"/>
        </index>
    </table>
    <!-- Extends core tables with a payment-method summary column so it's visible in the
         admin Sales > Orders grid without a per-row Stripe API call. Populated once at
         order-creation time (OrderCreationService) and copied into sales_order_grid by
         Magento's own grid-sync indexer/triggers, same mechanism stripe/module-payments uses. -->
    <table name="sales_order" resource="sales" comment="Sales Order">
        <column xsi:type="varchar" name="stripehostedcheckout_payment_method" nullable="true" length="255"
                comment="Stripe Hosted Checkout Payment Method Summary"/>
    </table>
    <table name="sales_order_grid" resource="sales" comment="Sales Order Grid">
        <column xsi:type="varchar" name="stripehostedcheckout_payment_method" nullable="true" length="255"
                comment="Stripe Hosted Checkout Payment Method Summary"/>
    </table>
</schema>
